{
    "name": "template",
    "private": true,
    "version": "0.0.0",
    "scripts": {
        "install": "cd server ; npm install ; cd ../client ; npm install",
        "start": "cd client ; npm run build ; cd .. ; node server/"
    }
}